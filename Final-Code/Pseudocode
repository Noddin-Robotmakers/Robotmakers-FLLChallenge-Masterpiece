from pybricks.hubs import InventorHub
from pybricks.pupdevices import Motor, ColorSensor, UltrasonicSensor
from pybricks.parameters import Button, Color, Direction, Port, Side, Stop
from pybricks.robotics import DriveBase
from pybricks.tools import wait, StopWatch

hub = InventorHub()

# mission one = drive to mission one, lower the vertical bar, raise back up, drive back home.
# mission two = drive to the mission, lower the vertical bar (x) times, raise it, drive back home.
# mission three = drive to the mission, lower the vertical bar, lift it back up, drive back home.
# mission ten = drive to the mission, raise forklift/bar, lower, drive back home.

